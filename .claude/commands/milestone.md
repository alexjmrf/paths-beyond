Leia `PROGRESS.md` para saber o milestone atual e leia a seção correspondente de `docs/spec/09-roadmap.md`.

Depois leia **apenas** os arquivos de `docs/spec/` relevantes para este milestone.

Então:

1. Apresente um plano curto: arquivos que vai criar, ordem, e quais testes escreverá primeiro. Espere minha aprovação.
2. Implemente somente o escopo deste milestone. Nada de milestones futuros.
3. Escreva os testes antes da implementação nos pacotes `core` e `data`.
4. Ao terminar, rode `pnpm test` e `pnpm validate:data` e **cole a saída real**.
5. Liste um a um os critérios de aceite do milestone e aponte qual teste prova cada um.
6. Atualize `PROGRESS.md`.

Se encontrar uma decisão de design que a spec não cobre, pare e pergunte. Não invente.
