{
  "name": "paths-beyond",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "pnpm -r --if-present run typecheck",
    "lint": "node scripts/check-no-random.mjs",
    "validate:data": "pnpm --filter @paths-beyond/data run validate:data",
    "sim": "node -e \"console.error('pnpm sim: ainda não implementado (chega em M1/M2). Ver docs/spec/09-roadmap.md'); process.exit(1)\"",
    "balance": "node -e \"console.error('pnpm balance: ainda não implementado (chega em M8). Ver docs/spec/09-roadmap.md'); process.exit(1)\""
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
